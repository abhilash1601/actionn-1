name: My first Workflow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/Checkout@4

      - name: Welcome message
        run: echo "my first git hub"

      - name: List files
        run: ls

      - name: Read files
        run: cat README.md
