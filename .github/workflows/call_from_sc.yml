name: call from script
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/Checkout@v4

      - name: Executing shell
        run: |
          chmod +x acci_shell.sh
          ./acci_shell.sh

